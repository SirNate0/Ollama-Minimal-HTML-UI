// (Deprecated) lightweight adapter — the project now uses the
// official @mdit/plugin-katex browser build saved at
// `external/plugin-katex-browser.js`. This file is intentionally
// left as a small marker to avoid confusion.

export default {};
